export interface NinjaToasterQueue {
    until: () => boolean;
    callback: () => void;
}
export declare function createRenderQueue(): {
    add: (item: NinjaToasterQueue) => () => void;
    remove: (item: NinjaToasterQueue) => void;
    clear: () => void;
};
export type NinjaToasterRenderQueue = ReturnType<typeof createRenderQueue>;
